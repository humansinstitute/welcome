# Server configuration
PORT=3000
MODE=dev

# Database
DB_PATH=welcome.sqlite

# Nostr relays (comma-separated)
NOSTR_RELAYS=wss://relay.primal.net,wss://nos.lol,wss://relay.damus.io

# Admin npub for managing invite codes (optional)
# Users logging in with this npub will see the admin panel
ADMIN_NPUB=npub1...

# Welcome's private key for signing key teleport events
# Required for key teleport functionality (nsec or 64-char hex)
WELCOME_PRIVKEY=nsec1...
